# Maintainer: Nick Schuch <nick.schuch@skpr.io>

pkgname=php8.5-opentelemetry
_pkgreal=opentelemetry
pkgver=1.2.1
pkgrel=1
provides="php-opentelemetry=8.5"
pkgdesc="High-quality, ubiquitous, and portable telemetry to enable effective observability."
url="http://pecl.php.net/package/$_pkgreal"
arch="all"
license="PHP-3"
depends="php8.5-common>=8.5.0 php8.5-common<8.6.0"
makedepends="php8.5-dev>=8.5.0 php8.5-dev<8.6.0 autoconf"
source="http://pecl.php.net/get/$_pkgreal-$pkgver.tgz"
builddir="$srcdir/$_pkgreal-$pkgver"

prepare() {
	default_prepare || return 1
}

build() {
	cd "$builddir"
	phpize || return 1
	./configure --prefix=/usr --with-php-config=/usr/bin/php-config || return 1
    make || return 1
}

package() {
	mkdir -p "$pkgdir" || return 1
    install -d $pkgdir/ || return 1
    make -C "$builddir" INSTALL_ROOT="$pkgdir" install || return 1
    install -d "$pkgdir"/etc/php/conf.d || return 1
    echo "extension=$_pkgreal" > "$pkgdir"/etc/php/conf.d/01_$_pkgreal.ini || return 1
}

sha512sums="
d49c335ea4d2e570e021b93c2a235850ca6e2e88b593a8e30a0130427fb76d089b291ec407ff1b766aca96724d0ea75e86e798522852a3560eca07af6d56449c  opentelemetry-1.2.1.tgz
"
